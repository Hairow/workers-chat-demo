{
  "name": "edge-chat-demo",
  "description": "An edge chat service that runs on Cloudflare Workers using Durable Objects",
  "version": "1.0.0",
  "module": "./src/chat.mjs",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "predeploy": "wrangler d1 migrations apply my-db-name --remote",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "jose": "^6.2.8"
  },
  "devDependencies": {
    "wrangler": "^4.120.0"
  }
}
